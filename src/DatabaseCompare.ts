/**
 * 
 * Compares my current schemas with the on on my database and writes the result to a file when running as normal compare otherwise it returns the result so the update can use it and updates the db 
 * 
 */

export default class DatabaseCompare {

}